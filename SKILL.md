---
name: douni-full-base-sync
description: 完整扫描豆包用增 13 张机构执行表，把共享业务结构下的全部记录、可写字段和附件汇总到“全量表（AI维护）”。用于同步全量表、刷新总表、核对机构总和，也用于豆尼每半小时定时同步。
---

# 豆尼多维全量表同步

## 公共业务配置

本 Skill 保留处理流程、例外和确认门禁，不单独维护客户表地址、周文档、机构色值或调度时间。先定位豆尼仓库（包含 `config/business-sources.json`），在仓库目录只读执行：

```powershell
node scripts/business-context.mjs --skill douni-full-base-sync
```

需要周执行文档时加 `--date YYYY-MM-DD`。阅读返回的来源、公共口径和本 Skill 写入边界；若运行时已注入 `runtime/business-context.json`，直接读取该快照，不另取一份不同版本。源缺失时停止相关操作，不能翻旧文档猜地址。手动和定时均调用原执行器，配置不授予新的写表或外发权限。

修改来源在仓库 `config/business-sources.json`，修改共用专项/色块/权威口径在 `config/business-policy.json`。调度以现有 Daemon 为准。脱离仓库独立打包时必须带上述配置和执行依赖、注明快照日期；不能把本 Skill 单独复制后视为可运行。Skill 自带命令仍在其原目录执行；包装器通过 `DOUNI_WORKSPACE` 或当前仓库目录定位执行器，不依赖固定盘符。

## 唯一口径

- 来源是同一 Base 中全部名称符合“机构 X 焦尾 执行表”的机构表，当前固定至少 13 张，包含 12 家合作机构和“自建”。
- 全量表不再保存任何独立权威字段。提报审核、查重结果、提报客户反馈、选题/备注与其他业务字段一样，全部以机构表当前值为准。
- 每轮完整读取 13 张机构表的全部历史记录和全部共享业务字段：新增、修改、清空、删除和附件都同步到全量表。
- 共享字段结构已经包含纽微特新增的 B 站发布/授权/报价字段、小红书发布字段、推广功能、客户审核、备注等字段；这些字段与其他业务字段一样进入全量同步。
- 后续新增业务字段时，先补齐 13 张机构表与全量表的字段结构，再进入同步；不得继续保留某家机构独有、全量表缺失的可写业务字段。
- 父记录保存来源表 record_id，跨表复制会变成无效关系，因此不属于可同步业务字段。
- 公式、Lookup、自动编号等只读字段不写；星图/蒲公英报价截图与 B 站授权通过截图按唯一 ID 单独核对并复用同一 Base 内的附件 token。
- 数值结果兼容例外：机构“互动总和”为 `[赞]+[评]+[藏]+[转发]` 公式、全量表同字段为数字时，读取机构公式本轮计算结果写入全量表，不修改机构公式或表结构。仅接受有效非负数字，保留实际零与空值；公式错误或不支持的结果停止本轮，不转成零或清空。其他公式/字段类型不兼容仍拦截。

## 每轮前置镜像

正式全量同步前，必须先调用 douni-daily-submission-backfill：

1. 扫描“每日提报表”文件夹内全部日提报表；
2. 仅将已提交且唯一匹配合作的指定审核、反馈和选题字段从机构镜像到对应原日表，保护未提交日表的内审内容；不盲选同 ID 最新日表，不整行覆盖；
3. 回读日提报表一致后，才开始机构表到全量表同步；
4. 镜像失败时整轮停止，避免日提报副本与机构主账不一致。

未提交时同事在日表维护内审和选题；客户结果回填入口负责客户终审到机构及原日表。此镜像入口本身不从日表回机构，全量表仍只以机构表为来源。具体镜像字段和合作匹配采用仓库当前 `scripts/lib/daily-submission-lifecycle.mjs`，不另维护一套整行覆盖规则。

## 完整性门禁

每日提报表复制单选/多选字段时，先按原始选项数量完整读取分页，再仅在目标日表定义中合并完全同名选项，保留第一次出现的显示样式。不修剪名称或模糊合并，不改机构原表。空名称、重复整页、总数变化、分页提前结束仍拦截。全量表仅提供日表所需的“机构”字段定义，不校验或复制其无关字段选项。

审核结果镜像补齐选项时，仅完整读取、校验本次 payload 中确实有非空选项值的字段；回读也仅核对本次补齐的字段。未参与镜像的历史平台、行业等字段不触碰，不能因无关历史空选项阻断指定审核字段镜像。

“13 张机构表记录总和等于全量表”是首要结果，同时必须满足：

- 来源表不少于 13 张；
- 全局唯一 ID 无重复；
- 全量表记录数等于 13 张机构表记录数之和；
- 各机构记录分布一致；
- 所有可写字段逐字段回读一致，包含空值清除；
- 附件数量和 token 一致。

新增全量记录时，飞书会对请求中省略的字段套用目标表默认值。机构快照为空的可写字段不得只在创建请求中省略；取得新 `record_id` 后，必须用原生 OpenAPI 显式写入 `null` 并回读，清除全量表默认值。例如机构“端口”为空时，不能让全量表默认的 `PC` 冒充来源值。该规则同样适用于其他带默认值的共享字段。

这些校验只防止“数量对、内容错”，不构成字段保护。

## 本轮源快照与并发修改

机构表是持续协作中的实时表，正常改表不能让半小时同步失败。每轮按以下口径执行：

1. 写全量表前，先把 13 张机构表本轮读到的字段、附件和 record_id 固化为本地不可变来源快照；
2. 本轮结构化字段、链接、空值清除和附件核对只使用这份快照，不在写入或复核阶段重新读取实时机构表；
3. 快照完成后的新增和修改留到下一轮同步；
4. 如果某张机构表恰好在分页读取期间变化，仍按本轮已固化快照同步新增和修改，但暂缓删除该机构在全量表中的疑似陈旧记录，防止分页漂移造成误删；
5. 这类并发变化记为“延后收口”，不判定整轮失败。下一轮读取稳定后自动完成删除与总量收口；
6. `full-rebuild` 属于高风险整表重建，读取期间有机构变化时仍必须停止，不能套用延后删除。

因此，稳定读取的机构必须当轮严格相等；读取期间变化的机构允许暂时保留少量待确认旧记录，但不得产生缺失、重复、字段差异或附件不一致。执行报告必须写明变化机构数和延后删除数。

## 执行入口

```powershell
cd E:\豆尼·银手
npm run base:sync-full
```

入口固定执行：机构审核结果镜像到每日提报表、结构化全量扫描、附件全量核对、成功后更新表名。全量表保留记录 ID，禁止整表删除重建。

查重数据另有就绪凭据：当日日表候选回读成功后发布收集回执；当日14:00后完整结构化同步并逐字段核验成功时，固化历史输入并发布就绪指针。附件、表名和工作台导出仍照常执行，其失败不会撤销已核验的查重输入，也不把整轮全量同步改报成功。凭据不完整时查重继续暂停。

## 定时规则

- 执行时刻和启停由 Daemon 当前计划决定，不在 Skill 维护另一份时间表。
- 北京时间 14:00 的这一轮先创建当天独立提报表并固化 14:00 快照，再执行回传和全量同步；14:00 后新增的提报不进入当天查重批次。
- 新建 `<M月D日提报>` 时，以公共配置 `submissions.viewTemplate` 指定的表为版式基准，不再按旧默认模板自由生成。必须按顺序保留 `表格`、`⛔️今日机构提报+查重`、`✅今日提报客户`、`3W以下提报` 四个表格视图；日期筛选中的 `2026-09-02` 只作为模板占位，每天替换为当日业务日期。
- `⛔️今日机构提报+查重` 固定筛选：提报时间=当日、达人名称非空、达人量级不等于 `3w 以下`；`✅今日提报客户` 固定筛选：提报时间=当日、提报审核=内审通过、达人名称非空；`3W以下提报` 固定筛选：提报时间=当日、达人名称非空、项目/专项=`3W以下课程教学`。
- 三个业务视图均按提报时间倒序、按平台正序分组，字段显示与 配置中的版式模板 保持一致；客户视图在提报时间后额外显示提报客户时间。默认 `表格` 视图不筛选、不排序、不分组，并沿用当天模板的字段顺序；以后新增的共享字段追加在默认视图末尾，不得被隐藏。
- 建表后必须等待字段结构异步落库收口，并用同一份不可变字段快照配置和验收视图；回读核对视图名称与顺序、筛选、排序、分组和可见字段。对象属性的返回顺序不参与差异判断，但条件、排序优先级、分组优先级和可见字段等数组顺序仍须严格一致。飞书异步落库造成的短暂不一致允许在限定次数内重读，最终仍不一致才判定 14:00 建表失败并触发异常提醒，禁止带着旧模板继续查重。
- 每次成功或失败都写入 Daemon 和超梦回放；失败不更新时间戳表名。
- 失败时保留本轮 stdout/stderr 完整本地诊断；告警优先报告失败阶段和真实 stderr，不能用前置镜像中正常“保留历史版本”的输出尾部掩盖真正错误。
- 附件核对的只读分页请求遇到飞书明确标记 `api/server_error` 且 `retryable=true`（如 1663）时，保留原请求和 offset，等待 5 秒、10 秒有限重读，总计最多 3 次。不得因单页瞬时错误重新执行整轮写入；权限、参数、不可重试错误仍立即停止，最终失败保留原始错误与日志编号。
- 机构正常编辑或 revision 变化本身不再触发整轮失败；只有来源缺表、唯一 ID 冲突、真实写入失败或快照回读不一致才失败。
- 表名只在全部校验通过后更新为“全量表（AI维护-MM-DD-HH时mm分）”。
- 用户身份的飞书调用必须经过本机跨进程授权锁；瞬时 token 刷新竞态先在锁内复核和重试，不直接要求重新扫码。
