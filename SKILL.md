---
name: douni-full-base-sync
description: 完整扫描豆包用增 13 张机构执行表，把共享业务结构下的全部记录、可写字段和附件汇总到“全量表（AI维护）”。用于同步全量表、刷新总表、核对机构总和，也用于豆尼每半小时定时同步。
---

# 豆尼多维全量表同步

## 唯一口径

- 来源是同一 Base 中全部名称符合“机构 X 焦尾 执行表”的机构表，当前固定至少 13 张，包含 12 家合作机构和“自建”。
- 全量表不再保存任何独立权威字段。提报审核、查重结果、提报客户反馈、选题/备注与其他业务字段一样，全部以机构表当前值为准。
- 每轮完整读取 13 张机构表的全部历史记录和全部共享业务字段：新增、修改、清空、删除和附件都同步到全量表。
- 共享字段结构已经包含纽微特新增的 B 站发布/授权/报价字段、小红书发布字段、推广功能、客户审核、备注等字段；这些字段与其他业务字段一样进入全量同步。
- 后续新增业务字段时，先补齐 13 张机构表与全量表的字段结构，再进入同步；不得继续保留某家机构独有、全量表缺失的可写业务字段。
- 父记录保存来源表 record_id，跨表复制会变成无效关系，因此不属于可同步业务字段。
- 公式、Lookup、自动编号等只读字段不写；星图/蒲公英报价截图与 B 站授权通过截图按唯一 ID 单独核对并复用同一 Base 内的附件 token。

## 每轮前置镜像

正式全量同步前，必须先调用 douni-daily-submission-backfill：

1. 扫描“每日提报表”文件夹内全部日提报表；
2. 机构表作为权威源，把已完成客户审核、且自上次成功后新增或变化的记录镜像到对应最新日提报表；
3. 回读日提报表一致后，才开始机构表到全量表同步；
4. 镜像失败时整轮停止，避免日提报副本与机构主账不一致。

同事直接在机构表更新审核结果、选题和客户反馈；每日提报表只接收机构表副本，全量表仍直接以机构表为来源。每日提报表和全量表都不得反向覆盖机构表。

## 完整性门禁

“13 张机构表记录总和等于全量表”是首要结果，同时必须满足：

- 来源表不少于 13 张；
- 全局唯一 ID 无重复；
- 全量表记录数等于 13 张机构表记录数之和；
- 各机构记录分布一致；
- 所有可写字段逐字段回读一致，包含空值清除；
- 附件数量和 token 一致。

新增全量记录时，飞书会对请求中省略的字段套用目标表默认值。机构快照为空的可写字段不得只在创建请求中省略；取得新 `record_id` 后，必须用原生 OpenAPI 显式写入 `null` 并回读，清除全量表默认值。例如机构“端口”为空时，不能让全量表默认的 `PC` 冒充来源值。该规则同样适用于其他带默认值的共享字段。

这些校验只防止“数量对、内容错”，不构成字段保护。

## 本轮源快照与并发修改

机构表是持续协作中的实时表，正常改表不能让半小时同步失败。每轮按以下口径执行：

1. 写全量表前，先把 13 张机构表本轮读到的字段、附件和 record_id 固化为本地不可变来源快照；
2. 本轮结构化字段、链接、空值清除和附件核对只使用这份快照，不在写入或复核阶段重新读取实时机构表；
3. 快照完成后的新增和修改留到下一轮同步；
4. 如果某张机构表恰好在分页读取期间变化，仍按本轮已固化快照同步新增和修改，但暂缓删除该机构在全量表中的疑似陈旧记录，防止分页漂移造成误删；
5. 这类并发变化记为“延后收口”，不判定整轮失败。下一轮读取稳定后自动完成删除与总量收口；
6. `full-rebuild` 属于高风险整表重建，读取期间有机构变化时仍必须停止，不能套用延后删除。

因此，稳定读取的机构必须当轮严格相等；读取期间变化的机构允许暂时保留少量待确认旧记录，但不得产生缺失、重复、字段差异或附件不一致。执行报告必须写明变化机构数和延后删除数。

## 执行入口

```powershell
cd E:\豆尼·银手
npm run base:sync-full
```

入口固定执行：机构审核结果镜像到每日提报表、结构化全量扫描、附件全量核对、成功后更新表名。全量表保留记录 ID，禁止整表删除重建。

## 定时规则

- 每天 08:30–24:00，每 30 分钟运行一次。
- 北京时间 14:00 的这一轮先创建当天独立提报表并固化 14:00 快照，再执行回传和全量同步；14:00 后新增的提报不进入当天查重批次。
- 新建 `<M月D日提报>` 时，以 2026-09-02 的 `9月2日提报`（`tbl22w3sHamYDuQj`）为版式基准，不再按旧默认模板自由生成。必须按顺序保留 `表格`、`⛔️今日机构提报+查重`、`✅今日提报客户`、`3W以下提报` 四个表格视图；日期筛选中的 `2026-09-02` 只作为模板占位，每天替换为当日业务日期。
- `⛔️今日机构提报+查重` 固定筛选：提报时间=当日、达人名称非空、达人量级不等于 `3w 以下`；`✅今日提报客户` 固定筛选：提报时间=当日、提报审核=内审通过、达人名称非空；`3W以下提报` 固定筛选：提报时间=当日、达人名称非空、项目/专项=`3W以下课程教学`。
- 三个业务视图均按提报时间倒序、按平台正序分组，字段显示与 `9月2日提报` 保持一致；客户视图在提报时间后额外显示提报客户时间。默认 `表格` 视图不筛选、不排序、不分组，并沿用当天模板的字段顺序；以后新增的共享字段追加在默认视图末尾，不得被隐藏。
- 建表后必须回读核对视图名称与顺序、筛选、排序、分组和可见字段；任一项未成功应用则 14:00 建表失败并触发异常提醒，禁止带着旧模板继续查重。
- 每次成功或失败都写入 Daemon 和超梦回放；失败不更新时间戳表名。
- 机构正常编辑或 revision 变化本身不再触发整轮失败；只有来源缺表、唯一 ID 冲突、真实写入失败或快照回读不一致才失败。
- 表名只在全部校验通过后更新为“全量表（AI维护-MM-DD-HH时mm分）”。
- 用户身份的飞书调用必须经过本机跨进程授权锁；瞬时 token 刷新竞态先在锁内复核和重试，不直接要求重新扫码。
